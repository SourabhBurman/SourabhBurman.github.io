# Resend API key (server-side only; never use a NEXT_PUBLIC_ prefix).
RESEND_API_KEY=
# Sender address on your verified Resend domain.
CONTACT_FROM_EMAIL=
# Inbox that receives portfolio contact submissions.
CONTACT_TO_EMAIL=
